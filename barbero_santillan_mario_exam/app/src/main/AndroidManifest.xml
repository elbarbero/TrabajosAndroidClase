<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.examen.mario.barbero_santillan_mario_exam">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".vista.InicioActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".vista.Login" />
        <activity android:name=".vista.MenuUsuario" />
        <activity android:name=".vista.AltaRecetas" />
        <activity
            android:name=".vista.LeerRecetas"
            android:label="@string/title_activity_leer_recetas"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".vista.DatosReceta" />
        <activity android:name=".vista.Autor"></activity>
    </application>

</manifest>